## Create

```shell
pnpm create vue
```

## Description

vue-language-tools, starting from version 3.2.0, up to now (3.2.1).

The type declaration of the global custom directive in TypeScript fails on the tag attributes,<br>
but the hint can still exist in the JavaScript expressions within the template.
